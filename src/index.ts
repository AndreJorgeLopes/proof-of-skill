// proof-of-skill — TDD framework for AI agent skills
//
// Stub entry point. Real Node/TypeScript sources live under src/.
// Keeps `tsc` (build) and `tsc --noEmit` (typecheck) happy until first
// real module lands. Replace freely.
export {};
